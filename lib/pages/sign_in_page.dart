import 'package:flutter/material.dart';

class SignInPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'ini halaman sign in'), // untuk sign in page nya kita taruh teks saja terlebih dahulu, kemudian untuk router --main.dart--
      ),
    );
  }
}
